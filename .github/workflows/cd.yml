name: CD

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    name: Build and Deploy
    runs-on: ubuntu-latest

    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

    steps:
      - name: Checkout code
        uses: actions/checkout@v4.2.2

      - name: Set up Java 21
        uses: actions/setup-java@v4.7.0
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      - name: Make mvnw executable
        run: chmod +x mvnw

      - name: Build artifact
        run: ./mvnw clean package -DskipTests

      # ── Azure steps activated at deployment milestone ──────
      # - name: Log in to Azure Container Registry
      #   uses: docker/login-action@v3
      #   with:
      #     registry: ${{ secrets.ACR_LOGIN_SERVER }}
      #     username: ${{ secrets.ACR_USERNAME }}
      #     password: ${{ secrets.ACR_PASSWORD }}

      # - name: Build and push Docker image
      #   uses: docker/build-push-action@v5
      #   with:
      #     context: .
      #     push: true
      #     tags: ${{ secrets.ACR_LOGIN_SERVER }}/task-api:${{ github.sha }}

      # - name: Deploy to Azure Container Apps
      #   uses: azure/container-apps-deploy-action@v1
      #   with:
      #     acrName: ${{ secrets.ACR_NAME }}
      #     containerAppName: task-api
      #     resourceGroup: ${{ secrets.AZURE_RESOURCE_GROUP }}
      #     imageToDeploy: ${{ secrets.ACR_LOGIN_SERVER }}/task-api:${{ github.sha }}

      - name: CD placeholder
        run: echo "CD pipeline ready. Azure steps activated at deployment milestone."
